# TODO
* Figure out why the program exits quietly with unexpected input.
* Let arbnumber.h parse a double with "e" or "E" in it for exponent.
* Add support for a list of expressions, and some associated functions....
  ex: sum({2,4,5,7}) = 18
* Add documentation about what functions are supported
* Add store(idxNum, value) and recall(idxNum, value) for storing/recalling memory
* Add conditionals  ifeq(expr1, expr2, a, b) ifneq(expr1, expr2, a, b) ifzero(expr, a, b) ifnzero(expr, a, b)  iflt(expr1, expr2, a, b) ifgt(expr1, expr2, a, b)


